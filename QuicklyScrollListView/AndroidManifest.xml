<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="com.example"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="15" />
    <uses-permission android:name="android.permission.READ_CONTACTS"/>

    <application android:icon="@drawable/icon" android:label="@string/app_name">
       <!--  <activity android:name=".QuicklyScrollListViewActivity" -->
        <!--   <activity android:name=".contact.ContactActivity" -->
     <!-- <activity android:name=".db.TestDatabaseActivity"> -->
         <activity android:name=".todos.TodosOverviewActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>  
        <activity
            android:name=".todos.TodoDetailActivity"
            android:windowSoftInputMode="stateVisible|adjustResize" >
        </activity>
  <provider
      android:name=".contentprovider.MyTodoContentProvider"
      android:authorities="com.example.contentprovider" >
   </provider>
    </application>
</manifest>